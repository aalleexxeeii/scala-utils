rules = [
  # regulation for tenant A
  {
    if {
      tenant = A
    }
    then {
      regulation = ES
    }
  },
  # engine for chess
  {
    if {
      game = chess
    }
    then {
      engine = chess
    }
  },
  # rewritten engine for mobile chess
  {
    if {
      client = mobile
      game = chess
    }
    // by reference
    then = external
  }
]

cfg {
  # example of externalized configuration
  external {
    engine = chess.new
    game = chess-new
  }
}
